[profile.default]
slow-timeout = { period = "1s", terminate-after = 2 }

# Show test output for hanging tests
failure-output = "immediate"
success-output = "never"

# Run tests in parallel but limit concurrency for simulation tests
test-threads = 1

# Timeout overrides for simulation tests
# All tests in the simulation folder get longer timeouts
[[profile.default.overrides]]
filter = 'test(/simulation/)'
slow-timeout = { period = "30s", terminate-after = 1 }

[profile.default.junit]
# Store test results
path = "target/nextest/default/junit.xml"
