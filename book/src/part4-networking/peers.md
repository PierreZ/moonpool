# Peers and Connections

<!-- toc -->

- The `Peer` abstraction: a resilient connection to a remote endpoint
- Message queuing during disconnection: messages buffered until reconnection
- Connection state machine: connecting → connected → disconnected → reconnecting
- Automatic reconnection with configurable backoff
