# Configuring the SimulationBuilder

<!-- toc -->

- Fluent API walkthrough: `.workload()`, `.processes()`, `.tags()`, `.invariant()`
- Iteration control: `UntilAllSometimesReached(1000)` vs `FixedCount(N)`
- Seed control: `.set_seed(seed)` for reproducing specific runs
- Topology: how IPs are assigned, how tags distribute roles
- Phases: structuring simulation into distinct stages
