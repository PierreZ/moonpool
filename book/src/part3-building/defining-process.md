# Defining a Process

<!-- toc -->

- Step-by-step: implement `Process` trait
- Set up a transport listener using providers
- Handle incoming requests in a loop
- Respect the shutdown token for graceful termination
- Register with `SimulationBuilder::processes(count, factory)`
